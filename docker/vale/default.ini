# Default Vale config used when no project-specific .vale.ini is provided.
# Vale's built-in rules only — no style packages needed.
# To use community styles (write-good, Microsoft, etc.) mount a custom
# .vale.ini and a StylesPath volume.

MinAlertLevel = suggestion

[*.md]
BasedOnStyles = Vale
